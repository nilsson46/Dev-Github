import java.util.Random;
import java.util.Scanner;

/*public class statiskaMetoder {
    public static void main(String[] args) {
        Random random = new Random();
        int result = random.nextInt(6) + 1;

        //kallar på funktionen.
        writeGreeting(); // VIKTIGT MED () Visar på att detta är ett metodanrop.(method invocation)
        write("Hey batman!"); //argument till funktionen

        askQuestion("how are you today?");

    }

    public static String askQuestion(String question) {
        Scanner scanner = new Scanner(System.in);

        System.out.println(question);
        String answer = scanner.nextLine();

        System.out.println("Answer" + answer);

        if (answer.equals("Good")) {
            System.out.println("Hey that's awesome!");
        } else {
            System.out.println("are you sure?");
            return answer;
        }
        public static void responseToMood () {

        }

        // void - returtyp(return type)
        //writeGreetings - metodnamn (method name)

        public static void writeGreeting () {
            System.out.println("Greetings fella");

        }

        // @param message (in-parameter)
        public static void write (String message){ //metodhuvud( method head) parametrar.
            // skall vara ett verb. beskriva en action. börja inte med tecken.

            //Metodkropp(method body)
            System.out.println(message);


        }
    }
} */



